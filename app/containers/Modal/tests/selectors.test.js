// import { selectModalDomain } from '../selectors';
// import { fromJS } from 'immutable';


// const selector = selectModalDomain();

describe('selectModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
