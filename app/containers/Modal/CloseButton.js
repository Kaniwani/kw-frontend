import styled from 'styled-components';

const CloseButton = styled.button`
  position: absolute;
  appearance: none;
  padding: .5em;
  top: 0;
  right: 0;
  cursor: pointer;
`;

export default CloseButton;
