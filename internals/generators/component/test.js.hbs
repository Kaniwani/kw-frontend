import React from 'react';
import { shallow } from 'enzyme';
import {{ properCase name }} from '../index';

describe('<{{ properCase name }} />', () => {
  it('should match baseline snapshot', () => {
    expect(shallow(<{{ properCase name }} />)).toMatchSnapshot();
  });
});
