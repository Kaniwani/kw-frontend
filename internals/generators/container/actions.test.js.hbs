import {
  TYPES,
  defaultAction,
} from '../actions';

describe('{{ upperCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of APP/{{ upperCase name }}/DEFAULT', () => {
      expect(defaultAction()).toEqual({ type: TYPES.DEFAULT });
      expect(defaultAction()).toMatchSnapshot();
    });
  });
});
