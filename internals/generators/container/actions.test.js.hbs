import {{ camelCase name }} from '../actions';

describe('{{ upperCase name }} actions', () => {
  describe('{{ camelCase name }}.load.request', () => {
    it('has a type of {{ upperCase name }}/LOAD/REQUEST', () => {
      expect({{ camelCase name }}.load.request()).toEqual({ type: '{{ upperCase name }}/LOAD/REQUEST' });
    });
    it('will set payload and meta from params', () => {
      const actual = {{ camelCase name }}.load.request('payloadval', 'metaval');
      const expected = {
        type: '{{ upperCase name }}/LOAD/REQUEST',
        payload: 'payloadval',
        meta: 'metaval'
      };
      expect(actual).toEqual(expected);
    });
  });
  describe('{{ camelCase name }}.load.success', () => {
    it('it has a type of {{ upperCase name }}/LOAD/SUCCESS', () => {
      expect({{ camelCase name }}.load.success()).toEqual({ type: '{{ upperCase name }}/LOAD/SUCCESS' });
    });
  });
  describe('{{ camelCase name }}.load.failure', () => {
    it('it has a type of {{ upperCase name }}/LOAD/FAILURE', () => {
      const anError = Error('herpaderp');
      expect({{ camelCase name }}.load.failure(anError))
        .toEqual({ type: '{{ upperCase name }}/LOAD/FAILURE', payload: anError, error: true });
    });
  });
  describe('{{ camelCase name }}.load.cancel', () => {
    it('it has a type of {{ upperCase name }}/LOAD/CANCEL', () => {
      expect({{ camelCase name }}.load.cancel()).toEqual({ type: '{{ upperCase name }}/LOAD/CANCEL' });
    });
  });
});
