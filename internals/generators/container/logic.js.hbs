import { createLogic } from 'redux-logic';
import {{ camelCase name }} from './actions';

// Individual exports for testing
export const defaultLogic = createLogic({
  type: {{ camelCase name }}.load.request, // action type logic listens for
  cancelType: {{ camelCase name }}.load.cancel, // actions to cancel on
  // latest: true, // use response for latest request only
  /* other less used options: debounce, throttle */

  processOptions: {
    successType: {{ camelCase name }}.load.success, // apply action type/creator for success
    failType: {{ camelCase name }}.load.failure, // apply action type/creator on errors
  },

  process({ getState, action: { payload } }) {
    return new Promise((resolve) => setTimeout(() => resolve(payload), 500));
  },
});

export default [
  defaultLogic,
];
