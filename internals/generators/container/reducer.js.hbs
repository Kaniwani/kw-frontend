import { handleActions } from 'redux-actions';
import { TYPES } from './actions';

const initialState = {};

const {{ camelCase name }}Reducer = handleActions({
  [TYPES.DEFAULT]: (state, { payload, meta, error }) => {
    if (error) {
      // TODO: global redux logic should intercept errors
      console.error(`Error in ${TYPES.DEFAULT}`, error); // eslint-disable-line no-console
    }
    return { ...state, ...payload, ...meta };
  },
}, initialState);

export default {{ camelCase name }}Reducer;
