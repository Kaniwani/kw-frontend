import React from 'react';
import { mount } from 'enzyme';
import {{ properCase name }} from '../index';

describe('<{{ properCase name }} />', () => {
  it('should match baseline snapshot', () => {
    expect(mount(<{{ properCase name }} />)).toMatchSnapshot();
  });
});
