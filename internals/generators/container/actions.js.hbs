import actionTypeCreator, { SYNC } from 'redux-action-types-creator';
import { createAction } from 'redux-actions';

export const TYPES = actionTypeCreator('APP/{{ upperCase name  }}')({
  DEFAULT: SYNC,
});

export const defaultAction = createAction(TYPES.DEFAULT, (payload) => payload, (meta) => meta);
